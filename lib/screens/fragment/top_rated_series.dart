import 'package:flutter/material.dart';

class TopRatedSeries extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Top Rated Series',  style: TextStyle(
        color: Colors.black,
      ),),
    );
  }
}
